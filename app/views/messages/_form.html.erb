<%= form_with(model: message, url: lobby_messages_path(@lobby || message&.lobby), local: true, id: "messages_form") do |f| %>
  <div>
    <%= f.text_area :body, rows: 3 %>
  </div>
  <div>
    <%= f.submit "Send" %>
  </div>
<% end %>
