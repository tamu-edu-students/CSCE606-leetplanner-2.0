<!DOCTYPE html>
<html>
  <head>
    <title>Leetcode Planner</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%= javascript_importmap_tags %>
  <!-- Use importmap module tag for whiteboard (was javascript_include_tag which failed to locate asset) -->
  <%= javascript_import_module_tag "whiteboard", defer: true %>
    <script>document.documentElement.classList.add('js-enabled');</script>
  </head>
  <body>
   <%= render "shared/flash" %>
    <main>
      <%= yield %> <%# This renders the specific page content %>
    </main>
  </body>
</html>
